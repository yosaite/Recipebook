@model IEnumerable<Recipe>
@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h1 class="display-4">Witejcie</h1> <br />
    <p>Tukej bydzie kejś fajnŏ ksiōnżka z receptami.</p>
    <p>Może niy nojlepszŏ ale jako tako.</p>
</div>
<div class="album py-5 bg-light">
    <div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-4">
                    <div class="card mb-4 box-shadow">
                        @if (@item.Images.Count != 0)
                        {
                            <img class="card-img-top" alt="Card image cap" src="@item.Images.First().Path" width="200" height="200">
                        }
                        else
                        {
                            <h1>Tu ni ma zdjonca</h1>
                        }
                        <div class="card-body">
                            <h5 class="card-title">@item.Name</h5>

                            <p class="card-text">@item.Description.Substring(0,15)...</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                                </div>
                                <small class="text-muted">9 mins</small>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>


